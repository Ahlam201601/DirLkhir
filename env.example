# PostgreSQL (Neon, Supabase, etc.) — connection pooling for the app.
DATABASE_URL="postgresql://..."

# Direct connection. Used for migrations (drizzle-kit push).
DIRECT_URL="postgresql://..."

# Better Auth — secret MUST be at least 32 characters (e.g. openssl rand -base64 32)
BETTER_AUTH_SECRET="your-secret-key-at-least-32-chars-long"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"